name: ci

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize]

env:
  DATABASE_URL: ${{ secrets.DATABASE_URL }}

jobs:
  lint-js:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          token: ${{ secrets.GH_PERSONAL_TOKEN }}

      - name: Setup project
        uses: ./.github/actions/setup-project
        with:
          database_url: ${{ secrets.DATABASE_URL }}

      - name: Lint JS
        run: pnpm lint:js

  lint-style:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          token: ${{ secrets.GH_PERSONAL_TOKEN }}

      - name: Setup project
        uses: ./.github/actions/setup-project
        with:
          database_url: ${{ secrets.DATABASE_URL }}

      - name: Lint Style
        run: pnpm lint:style

  type-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          token: ${{ secrets.GH_PERSONAL_TOKEN }}

      - name: Setup project
        uses: ./.github/actions/setup-project
        with:
          database_url: ${{ secrets.DATABASE_URL }}

      - name: Type check
        run: pnpm typeCheck

  test:
    runs-on: ubuntu-latest
    # Prisma 経路のテストは実 DB を通すので、本番ではなく使い捨ての Postgres へ向ける
    env:
      DATABASE_URL: postgresql://root:root@localhost:5432/attt_test
      TEST_DATABASE_URL: postgresql://root:root@localhost:5432/attt_test
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true
          token: ${{ secrets.GH_PERSONAL_TOKEN }}

      - name: Start test database
        run: |
          docker compose -f packages/prisma/compose.yml up -d db
          until docker compose -f packages/prisma/compose.yml exec -T db pg_isready -U root; do
            sleep 1
          done
          docker compose -f packages/prisma/compose.yml exec -T db psql -U root -d postgres -c 'CREATE DATABASE attt_test' || true

      - name: Setup project
        uses: ./.github/actions/setup-project
        with:
          database_url: postgresql://root:root@localhost:5432/attt_test

      - name: Migrate database
        run: pnpm prisma migrate deploy

      - name: Test
        run: pnpm test

  ci:
    if: ${{ always() }}
    needs: [lint-js, lint-style, type-check, test]
    runs-on: ubuntu-latest
    steps:
      - name: Check results
        run: |
          test "${{ needs.lint-js.result }}" = "success"
          test "${{ needs.lint-style.result }}" = "success"
          test "${{ needs.type-check.result }}" = "success"
          test "${{ needs.test.result }}" = "success"
